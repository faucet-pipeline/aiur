#!/usr/bin/env node
"use strict";

let { faucetDispatch } = require("faucet-pipeline-core");
let parseCLI = require("../lib/cli");

let { referenceDir, config, options } = parseCLI();

faucetDispatch(referenceDir, config, options);
